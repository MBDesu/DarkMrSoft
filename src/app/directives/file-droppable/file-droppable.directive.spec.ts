import { FileDroppableDirective } from './file-droppable.directive';

describe('DraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDroppableDirective();
    expect(directive).toBeTruthy();
  });
});
